# Server Configuration
PORT=3001

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=naga_stall

# CORS Configuration (Optional - defaults are set in cors.js)
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# Environment
NODE_ENV=development